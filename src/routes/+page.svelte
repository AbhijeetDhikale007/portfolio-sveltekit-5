<script lang="ts">
    import { fly } from "svelte/transition";
    import { Header } from "$routes"
    import { Main } from "$store";
    
    import './app.scss'
</script>

<header class="fixed top-0 z-2" in:fly={{y: -100, duration: 1000}}>
    <Header />
</header>
<main class='z-1'>
    <svelte:component this={$Main}></svelte:component>
</main>

<style lang='scss'>
    main {
        @apply flex justify-center items-center min-h-[88vh] w-[99vw];
    }
</style>